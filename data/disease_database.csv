import sys
import os
import pandas as pd
import streamlit as st

ROOT_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))
if ROOT_DIR not in sys.path:
    sys.path.insert(0, ROOT_DIR)

st.set_page_config(page_title="Disease Database · CropGuard", page_icon="📚", layout="wide")

st.title("📚 Crop Disease Knowledge Base")
st.write("Browse common crop diseases, early symptoms, and organic/chemical remedies.")

@st.cache_data
def load_database():
    csv_path = os.path.join(ROOT_DIR, "data", "disease_database.csv")
    try:
        # on_bad_lines='skip' ensures malformed lines won't crash Pandas
        return pd.read_csv(csv_path, on_bad_lines='skip', engine='python')
    except Exception:
        # Fallback dataset in case CSV file is corrupted or missing
        return pd.DataFrame([
            {
                "Crop": "Wheat",
                "Disease": "Leaf Rust",
                "Symptoms": "Reddish-orange pustules on leaf surface",
                "Treatment": "Spray Propiconazole or Copper Fungicide"
            },
            {
                "Crop": "Cotton",
                "Disease": "Leaf Curl Virus",
                "Symptoms": "Upward curling of leaves and thickened veins",
                "Treatment": "Control whitefly vector using Imidacloprid"
            },
            {
                "Crop": "Rice",
                "Disease": "Bacterial Leaf Blight",
                "Symptoms": "Water-soaked lesions turning yellow-white streaks",
                "Treatment": "Apply Copper Oxychloride + Streptocycline"
            }
        ])

df = load_database()

search_query = st.text_input("🔍 Search disease or crop name:", "")

if search_query:
    filtered_df = df[df.apply(lambda row: row.astype(str).str.contains(search_query, case=False).any(), axis=1)]
    st.dataframe(filtered_df, use_container_width=True)
else:
    st.dataframe(df, use_container_width=True)
